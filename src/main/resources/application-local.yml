spring:
  # MySQL - 로컬 Docker
  datasource:
    url: jdbc:mysql://localhost:3306/anabada?useSSL=false&characterEncoding=UTF-8&serverTimezone=Asia/Seoul&allowPublicKeyRetrieval=true
    username: root
    driver-class-name: com.mysql.cj.jdbc.Driver
    hikari:
      maximum-pool-size: 5
      minimum-idle: 2
      connection-timeout: 30000

  jpa:
    hibernate:
    show-sql: true

  # Redis - 로컬 Docker
  data:
    redis:
      host: localhost
      port: 6379
      password:             # 로컬은 비밀번호 없음
      timeout: 2000ms

  # 이메일 - 로컬은 콘솔 출력 (실제 발송 X)
  mail:
    host: smtp.gmail.com
    port: 587
    username: smileksa13@gmail.com
    password: vrhkzjkuhpzluwzg
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

# 로컬 로그 (SQL 쿼리 전체 출력)
logging:
  level:
    com.example.anabadabackend: DEBUG
    org.hibernate.SQL: DEBUG
    org.springframework.mail: DEBUG

# 로컬에서는 이메일 실제 발송하지 않고 콘솔에만 출력
email-auth:
  mock-send: false  # true면 실제 발송 안 하고 로그로만 출력